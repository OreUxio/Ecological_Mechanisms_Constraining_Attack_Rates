/* $Id: mp_sug_numthreads.c 48 2005-12-15 06:48:28Z cvsrep $ */
/* at runtime, there are complaints that this symbol does not exist,
   so we define it here with a probably safe value */
long long __mp_sug_numthreads=0; /* dummy */
